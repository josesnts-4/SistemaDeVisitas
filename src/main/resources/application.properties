server.port=${PORT:8080}
server.forward-headers-strategy=framework
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.same-site=LAX

spring.application.name=SistemaDeVisitas

# 🚀 Datasource (Railway)
spring.datasource.url=jdbc:postgresql://crossover.proxy.rlwy.net:36515/railway?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=GmKExDIiaiSxHmAkNUaeaAtVvGZfkEkb
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway (migrações em db/migration)
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Actuator (monitoramento)
management.endpoints.web.exposure.include=health,info
